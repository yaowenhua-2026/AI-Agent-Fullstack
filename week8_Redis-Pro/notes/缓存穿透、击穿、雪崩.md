# 缓存穿透、击穿、雪崩 - 学习总结

## 学习时间
2026-05-13

---

## 一、实验环境

| 组件 | 版本/配置 |
|------|----------|
| Spring Boot | 3.5.14 |
| Redis | 7.4.2 |
| MySQL | 8.0 |
| MyBatis Plus | 3.5.9 |
| JDK | 17 |
| 测试工具 | JMeter 5.6.3 |

---

## 二、三种问题对比

| 问题 | 发生场景 | 影响 | 解决方案 |
|------|----------|------|----------|
| **缓存穿透** | 查询不存在的数据 | 请求直接打到 DB | 布隆过滤器、缓存空值 |
| **缓存击穿** | 热点 key 过期 | 瞬间大量请求打到 DB | 互斥锁、逻辑过期 |
| **缓存雪崩** | 大量 key 同时过期 | DB 压力骤增 | 随机过期时间、缓存预热 |

---

## 三、核心代码实现

### 3.1 空值缓存（防穿透）

```java
public Product getProductById(Long id) {
    String key = "product:" + id;
    String json = redisTemplate.opsForValue().get(key);
    
    if (json != null) {
        if ("null".equals(json)) return null;  // 命中空值缓存
        return JSON.parseObject(json, Product.class);
    }
    
    Product product = productMapper.selectById(id);
    
    if (product == null) {
        // 缓存空值，过期时间 60 秒
        redisTemplate.opsForValue().set(key, "null", 60, TimeUnit.SECONDS);
        return null;
    } else {
        redisTemplate.opsForValue().set(key, JSON.toJSONString(product), 300, TimeUnit.SECONDS);
        return product;
    }
}
3.2 互斥锁（防击穿）
java
public Product getProductByIdWithLock(Long id) {
    String key = "product:" + id;
    String lockKey = "lock:product:" + id;
    
    // 1. 查缓存
    String json = redisTemplate.opsForValue().get(key);
    if (json != null) {
        if ("null".equals(json)) return null;
        return JSON.parseObject(json, Product.class);
    }
    
    // 2. 尝试获取锁
    Boolean locked = redisTemplate.opsForValue()
            .setIfAbsent(lockKey, "1", 10, TimeUnit.SECONDS);
    
    if (Boolean.TRUE.equals(locked)) {
        try {
            // 3. 双重检查
            String cached = redisTemplate.opsForValue().get(key);
            if (cached != null) {
                if ("null".equals(cached)) return null;
                return JSON.parseObject(cached, Product.class);
            }
            
            // 4. 查数据库
            Product product = productMapper.selectById(id);
            if (product == null) {
                redisTemplate.opsForValue().set(key, "null", 60, TimeUnit.SECONDS);
                return null;
            } else {
                redisTemplate.opsForValue().set(key, JSON.toJSONString(product), 300, TimeUnit.SECONDS);
                return product;
            }
        } finally {
            // 5. 释放锁
            redisTemplate.delete(lockKey);
        }
    } else {
        // 6. 没拿到锁，等待后重试
        try { Thread.sleep(50); } catch (InterruptedException e) {}
        return getProductByIdWithLock(id);
    }
}
3.3 随机过期时间（防雪崩）
java
public void batchSetProduct(List<Product> products) {
    Random random = new Random();
    int baseExpire = 3600;      // 基础过期时间 1 小时
    int randomRange = 1800;     // 随机范围 ±30 分钟
    
    for (Product product : products) {
        String key = "product:" + product.getId();
        int expire = baseExpire + random.nextInt(randomRange);
        redisTemplate.opsForValue().set(key, JSON.toJSONString(product), expire, TimeUnit.SECONDS);
    }
}
四、测试结果
4.1 空值缓存
bash
curl http://localhost:8080/product/999   # 第一次，查数据库
curl http://localhost:8080/product/999   # 第二次，直接返回空
Redis 数据	结果
GET product:999	"null"
TTL product:999	约 60 秒
4.2 互斥锁（JMeter 50 并发）
指标	结果
总请求数	50
数据库查询次数	1 次
缓存命中次数	49 次
平均响应时间	4 ms
4.3 Redis 最终数据
bash
127.0.0.1:6379> GET product:1
{"id":1,"name":"测试商品","price":99.0}

127.0.0.1:6379> TTL product:1
299
五、三大方案对比
方案	接口	解决的问题	核心机制
空值缓存	GET /product/{id}	缓存穿透	缓存 "null"
互斥锁	GET /product/lock/{id}	缓存击穿	setIfAbsent 分布式锁
随机过期时间	POST /product/batch/preload	缓存雪崩	过期时间 + 随机偏移
六、面试速答
问题	答案
缓存穿透怎么解决？	布隆过滤器、缓存空值
缓存击穿怎么解决？	互斥锁、逻辑过期
缓存雪崩怎么解决？	随机过期时间、缓存预热
互斥锁的缺点？	影响性能，可能死锁
布隆过滤器特点？	一定不存在，可能存在
七、一句话总结
空值缓存拦穿透，互斥锁挡击穿，随机过期防雪崩，三大方案保数据库。